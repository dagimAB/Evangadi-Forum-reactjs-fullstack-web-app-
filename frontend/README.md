# Frontend placeholder

This folder is reserved for the React frontend app.

When you initialize the frontend, create a separate `package.json` here, for example with `npx create-react-app .` or `npm init react-app .`.

Current backend files remain at the project root and continue to use the root `package.json`.
